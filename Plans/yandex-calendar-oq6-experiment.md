# План эксперимента OQ-6 / OQ-7

**Цель:** выяснить, как Яндекс Календарь в CalDAV отличает **событие типа «Задача»** от **«Встреча»**, и как читать признак **«выполнена»**.  
**Связь:** BRD v1.1, блокер реализации create/sync.  
**Эталон UI:** диалог «Новое событие» → вкладка «Задача», календарь «Мои события».

**Успех эксперимента:** таблица «свойство → значение» + минимальный `PUT`, после которого в веб-UI запись открывается как **Задача**, не как Встреча и не как элемент `todos-*`.

---

## 0. Подготовка (15–30 мин)

1. В `.env.local` (не в git):
   - `YANDEX_CALENDAR_EMAIL`
   - `YANDEX_OAUTH_TOKEN` (scope `calendar:all`)
   - `YANDEX_EVENTS_CALENDAR_ID` (например `events-35737546`)
2. Проверить доступ:
   ```bash
   curl -s -X PROPFIND "https://caldav.yandex.ru/calendars/${EMAIL}/" \
     -H "Authorization: OAuth ${TOKEN}" \
     -H "Depth: 1" -H "Content-Type: application/xml" \
     --data '<?xml version="1.0"?><d:propfind xmlns:d="DAV:"><d:prop><d:displayname/></d:prop></d:propfind>'
   ```
3. Зафиксировать URL коллекции событий:  
   `https://caldav.yandex.ru/calendars/<email>/<events-id>/`
4. Удобно иметь рядом открытый [calendar.360.yandex.ru](https://calendar.360.yandex.ru/) под тем же аккаунтом.

---

## 1. Эталон «из UI» (чтение, без PUT)

**Идея:** сначала посмотреть, что Яндекс сам пишет в `.ics`, когда пользователь создаёт запись руками.

| Шаг | Действие | Результат записать |
|-----|----------|-------------------|
| 1.1 | В UI: «Новое событие» → **Встреча** → название `EXP-MEETING-001`, завтра 10:00–11:00, календарь «Мои события» → Создать | UID / ссылка |
| 1.2 | В UI: «Новое событие» → **Задача** → название `EXP-TASK-001`, завтра 12:00–13:00, тот же календарь → Создать | UID / ссылка |
| 1.3 | Опционально: задача из списка задач (`todos`), название `EXP-VTODO-001` | чтобы убедиться, что это **другой** путь |
| 1.4 | Через CalDAV `REPORT` / `GET` скачать оба (три) `.ics` | сырой текст |
| 1.5 | Diff `EXP-MEETING` vs `EXP-TASK` | список отличающихся свойств |

**Как найти объект после создания в UI:**
- `REPORT` `calendar-query` по диапазону дат эксперимента; или
- экспорт / поиск по `SUMMARY:EXP-TASK-001`.

**На что смотреть в diff (чеклист):**

- `CATEGORIES`, `CLASS`, `STATUS`, `TRANSP`
- все строки `X-…` / `X-YANDEX-…` / `X-MICROSOFT-…`
- `PRIORITY`, `PERCENT-COMPLETE`, `COMPLETED` (если вдруг есть у VEVENT)
- наличие/отсутствие `ATTENDEE`, `LOCATION`, `X-TELEMOST-*`
- одинаковый ли `BEGIN:VEVENT` у обоих (ожидаем да для встречи и задачи-события)
- у `EXP-VTODO-001` — ожидаем `BEGIN:VTODO` и коллекцию `todos-*`

**Выход шага 1:** гипотеза вида  
«тип Задача = свойство `X-FOO:BAR`» (или набор свойств).

---

## 2. Проверка «выполнена» (OQ-7)

На событии-задаче из UI:

| Шаг | Действие |
|-----|----------|
| 2.1 | Отметить `EXP-TASK-001` выполненной в веб-UI |
| 2.2 | Снова `GET` того же `.ics` |
| 2.3 | Diff до/после: какое поле появилось/изменилось (`STATUS`, `COMPLETED`, `PERCENT-COMPLETE`, `X-…`) |
| 2.4 | Снять выполнение выполнения → ещё один `GET` → что откатывается |

**Выход шага 2:** правило для sync Done в BRD (AC-3.3).

---

## 3. Синтез через CalDAV (запись)

Цель — повторить UI только через API.

| Шаг | Действие | Критерий |
|-----|----------|----------|
| 3.1 | `PUT` минимальный VEVENT **без** найденного маркера (как обычная встреча), `SUMMARY=EXP-PUT-MEETING` | В UI = **Встреча** |
| 3.2 | `PUT` тот же шаблон **+** кандидат-маркер из шага 1, `SUMMARY=EXP-PUT-TASK` | В UI = **Задача** |
| 3.3 | Если 3.2 не сработал — перебор кандидатов по одному (не пачкой) | Найти минимальный достаточный набор |
| 3.4 | `PUT` с маркером задачи, но в путь `todos-*` (негативный контроль) | Не наш целевой сценарий; зафиксировать отличие |

Шаблон PUT (подставить маркер после шага 1):

```text
BEGIN:VCALENDAR
VERSION:2.0
PRODID:-//MindMap//OQ6//RU
BEGIN:VEVENT
UID:<uuid>
DTSTAMP:<utc>
DTSTART;TZID=Asia/Yekaterinburg:20260822T120000
DTEND;TZID=Asia/Yekaterinburg:20260822T130000
SUMMARY:EXP-PUT-TASK
DESCRIPTION:oq6
<СЮДА_МАРКЕР_ТИПА_ЗАДАЧА>
END:VEVENT
END:VCALENDAR
```

**Критерий приёмки эксперимента (не приложения):**  
открыть `EXP-PUT-TASK` в UI → вкладка/тип **«Задача»**, календарь «Мои события».

---

## 4. Фиксация результата

Заполнить и вставить в BRD (закрыть OQ-6 / OQ-7) или в конец этого файла:

```markdown
### Результат эксперимента (дата: ____)

| Вопрос | Ответ |
|--------|--------|
| Компонент | VEVENT / другое: ____ |
| Коллекция | events-____ |
| Маркер типа «Задача» | `ИМЯ:ЗНАЧЕНИЕ` |
| Маркер «выполнена» | `ИМЯ:ЗНАЧЕНИЕ` |
| Минимальный рабочий PUT | см. gist / вложение |
| Что не сработало | … |
```

Приложить сырые `.ics` эталонов (без токенов) в `Plans/oq6-samples/` **только если** нет персональных данных в DESCRIPTION.

---

## 5. Порядок работ и время

| # | Блок | Оценка |
|---|------|--------|
| 0 | Подготовка env + PROPFIND | 15–30 мин |
| 1 | Эталоны из UI + diff | 30–60 мин |
| 2 | Выполнена / не выполнена | 15–30 мин |
| 3 | PUT-проверка гипотезы | 30–90 мин |
| 4 | Запись в BRD | 15 мин |

**Итого:** ~2–4 часа в один заход.

---

## 6. Риски

| Риск | Что делать |
|------|------------|
| Маркер только на стороне UI, CalDAV пишет «голый» VEVENT | Сравнить GET сразу после UI-create; если идентичны встрече и задаче — эскалация: тип не различим по CalDAV → менять AC / UX приёмки |
| Разный формат у 360 business vs личный аккаунт | Повторить на том же аккаунте, что будет в проде |
| Токен без нужных scope | Обновить OAuth, повторить PROPFIND |
| Случайный мусор в календаре | Имена только с префиксом `EXP-`; в конце удалить тестовые записи |

---

## 7. После успеха

1. Обновить BRD: закрыть OQ-6/OQ-7 конкретными полями.  
2. Добавить в `Plans/yandex-calendar-api.md` рабочий пример `PUT` события-задачи.  
3. Только тогда стартовать feature-ветку **от актуального `main`**.

**Пока OQ-6 открыт — код create/sync не писать.**

---

## Результат эксперимента (дата: 21.08.2026) — промежуточный

### Шаг 0 — OK
- CalDAV OAuth работает (`HTTP 207` на PROPFIND).
- Коллекции: `events-35737546` = «Мои события», `todos-7647328` = «Не забыть».

### Шаг 3 (CalDAV PUT) — выполнено без эталона из UI

Созданы тестовые `VEVENT` в `events-35737546` с кандидатами маркеров:

| SUMMARY | Что отправили | Что вернул GET |
|---------|---------------|----------------|
| EXP-PUT-MEETING | минимальный VEVENT | `VEVENT`, `CATEGORIES:Мои события`, `CLASS:PRIVATE`, `TRANSP:OPAQUE` |
| EXP-PUT-TASK-CAT | `CATEGORIES:TASK` | **сброшено** → снова `CATEGORIES:Мои события` |
| EXP-PUT-TASK-CAT-RU | `CATEGORIES:Задача` | **сброшено** |
| EXP-PUT-TASK-STATUS | `STATUS:NEEDS-ACTION` | **сброшено** |
| EXP-PUT-TASK-PCT | `PERCENT-COMPLETE:0` | **сброшено** |
| EXP-PUT-TASK-XYA / XYB | `X-YANDEX-EVENTTYPE` / `X-YANDEX-TYPE` | **сброшено** |
| EXP-PUT-TASK-TODO | busy+status | **сброшено** |
| EXP-PUT-COLOR | `COLOR:#FF0000` | **сброшено** |
| EXP-PUT-TRANSP | `TRANSP:TRANSPARENT` | **сохраняется** |
| EXP-PUT-CLASS-PUB | `CLASS:PUBLIC` | **сохраняется** |
| EXP-VTODO-001 | VTODO в `todos-*` | `BEGIN:VTODO`, `STATUS:NEEDS-ACTION`, `X-POS:1` (это список задач, не цель фичи) |

**Вывод A:** сервер Яндекса при `PUT` в `events-*` **нормализует** событие и **вырезает** произвольные `CATEGORIES` / `STATUS` / `X-YANDEX-*` / `COLOR`. Отличить «Задачу» от «Встречи» этими полями через CalDAV **нельзя** (по крайней мере так).

**Вывод B:** `VTODO` в `todos-*` — другой объект (список «Не забыть»), не вкладка «Задача» в «Новое событие».

### Шаг 1–2 — НЕ завершены (нужен UI)

В недельном `REPORT` по `events-*` у всех VEVENT набор «особых» полей один и тот же:  
`ATTENDEE`, `CATEGORIES`, `CLASS`, `COLOR`, `CONFERENCE`, `ORGANIZER`, `TRANSP`, `URL`, `X-TELEMOST-CONFERENCE`.  
**Отдельного свойства «тип=Задача» в уже лежащих событиях не видно.**

Без пары эталонов, созданных **вручную в веб-UI** (Встреча vs Задача), нельзя:

- подтвердить, пишет ли UI вообще различимый маркер в CalDAV;
- закрыть OQ-6 / OQ-7.

### Что сделать вам (5 минут)

В [calendar.360.yandex.ru](https://calendar.360.yandex.ru/) под тем же аккаунтом:

1. «Новое событие» → **Встреча** → название **`EXP-MEETING-001`**, 24.08.2026 10:00–11:00, календарь «Мои события» → Создать.  
2. «Новое событие» → **Задача** → название **`EXP-TASK-001`**, 24.08.2026 12:00–13:00, тот же календарь → Создать.  
3. Напишите здесь «готово» — агент скачает оба `.ics`, сделает diff и допишет результат.

Опционально для OQ-7: отметьте `EXP-TASK-001` выполненной в UI и снова напишите «готово (выполнена)».

### Черновой статус OQ

| ID | Статус |
|----|--------|
| OQ-6 | **Закрыт с негативным результатом** — см. ниже |
| OQ-7 | **Неприменим через CalDAV** для UI-типа «Задача» — объект не виден |

Тестовый мусор в календаре: все `EXP-PUT-*` и `EXP-VTODO-001` — можно удалить вручную после эксперимента.

---

## Результат после UI-эталонов (21.08.2026, продолжение)

### Эталоны в веб-UI
| Название | Тип в UI | Календарь | Время | Видно в CalDAV? |
|----------|----------|-----------|-------|-----------------|
| `EXP-MEETING-001` | Встреча | Мои события | 18:30–19:00 | **Да** — `VEVENT`, обычный GET/REPORT |
| `EXP-TASK-001` | Задача (скрин: галочка у названия, длительность **0 мин**) | Мои события | 18:30–18:30 | **Нет** |

### Проверки CalDAV по `EXP-TASK-001`
- `REPORT` с `time-range` на день / слот 18:30 — только встреча
- `REPORT` **без** time-range по всем VEVENT коллекции (~1473 SUMMARY) — **нет** `EXP-TASK-001`
- `REPORT` + `text-match` SUMMARY — **нет**
- `REPORT` по `todos-*` (VTODO/VEVENT) — **нет**
- В слоте 18:30 YEKT видна только `EXP-MEETING-001`

### Итог OQ-6 / OQ-7

| Вопрос | Ответ |
|--------|--------|
| Компонент UI «Задача» в «Новое событие» | **Не экспортируется в CalDAV** (не VEVENT в `events-*`, не VTODO в `todos-*`) |
| Можно ли отличить Задачу от Встречи по iCalendar? | **Нет через CalDAV** — объект задачи из UI отсутствует в протоколе |
| Можно ли создать «Задачу» (как во вкладке UI) через CalDAV PUT? | **Не подтверждено / практически нет:** маркеры срезаются; PUT даёт обычный VEVENT (= встреча в UI) |
| Что доступно через CalDAV | Обычные **встречи** (`VEVENT` в `events-*`) и **список задач** (`VTODO` в `todos-*`, UI «Не забыть») |

### Следствие для ТЗ (нужно решение заказчика)

Фича «создать в Календаре событие с типом Задача, как во вкладке UI» **не реализуема только через CalDAV**. Варианты:

1. **Сузить v1:** создавать обычный `VEVENT` (в UI будет «Встреча») — CalDAV работает.  
2. **VTODO** в `todos-*` («Не забыть») — это другой UX, не вкладка «Задача».  
3. **Искать другой API** Яндекса (непубличный/внутренний веб-API) — вне текущего плана, риски.  
4. **Отложить фичу**, пока нет поддерживаемого API для этого типа объекта.

