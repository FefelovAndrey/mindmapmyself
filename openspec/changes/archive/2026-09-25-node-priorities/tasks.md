# Tasks

## 1. Модель данных

- [x] 1.1 Добавить узлу `priority: 1 | 2 | 3 | null`. В схеме отсутствующее поле читать как `null`, значение вне 1–3 отклонять. Проставить `null` при создании узла, в корне документа по умолчанию и в импорте mmap. Обновить фикстуры тестов. Проверка: документ без поля `priority` проходит разбор схемы, у нового узла приоритет `null`.
- [x] 1.2 Проверка сценариев «Существующая карта без приоритетов» и «Новый узел без приоритета»: после загрузки старой карты и после Tab у узла нет цифры, в карточке «не указан».

## 2. Фильтр в логике дерева

- [x] 2.1 Расширить состояние фильтра значениями `'1' | '2' | '3' | 'none'`. Пустой список совпадает со всеми. Несколько значений — «или». `'none'` совпадает только с отсутствием приоритета. Условие складывается через «и» с ответственным и статусом. Предки совпадения остаются видимыми. Проверка: новые тесты Jest в `filterTree` на эти случаи зелёные, старые тесты фильтра не ломаются (`npm test`).
- [x] 2.2 Покрыть тестами логику сценариев «Галочка 1», «Галочки 1 и 2», «Только без приоритета», «Предки узла с приоритетом 1», «Приоритет 1 и статус Done» и пустой фильтр. Проверка: `npm test` проходит.

## 3. Карточка и отображение

- [x] 3.1 В карточке узла добавить список «Приоритет»: «не указан», 1, 2, 3. Смена уходит в то же автосохранение, что статус. Проверка сценариев «Задать приоритет 1», «Сменить 1 на 3», «Снять приоритет»: шапка доходит до «Сохранено», после обновления страницы значение на месте.
- [x] 3.2 В Outline показать цифру сразу перед именем; без приоритета ничего не рисовать. Приоритет 1 — залитый бейдж и лёгкая заливка строки. 2 и 3 — контурный бейдж без заливки. Статус, ответственный и срок остаются справа. У Done с приоритетом 1 видны и заливка, и бейдж Done. Проверка сценариев «Цифра в Outline», «Нет цифры без приоритета», «Приоритет 1 выделен в обоих режимах», «Приоритеты 2 и 3 без заливки», «Приоритет 1 у узла со статусом Done».
- [x] 3.3 На карте показать ту же цифру в шапке карточки рядом с номером, без новой строки. Заливка карточки только у приоритета 1. Проверка сценариев «Цифра на карте» и «Приоритет 1 выделен в обоих режимах» на вкладке «Карта».

## 4. Фильтр в полосе

- [x] 4.1 Добавить в общую полосу список «Приоритет» с галочками 1, 2, 3 и «Без приоритета». Пустой список подписан «Все». «Сбросить» снимает и его. Один фильтр действует в Outline и на карте, номера не меняются. Проверка сценариев «Пустой фильтр показывает всех», «Галочка 1», «Галочки 1 и 2», «Только без приоритета», «Сброс фильтра», «Предки узла с приоритетом 1», «Предки на карте», «Приоритет 1 и статус Done».

## 5. Сборка

- [x] 5.1 Прогнать `npm test` и `npm run build`. Проверка: оба завершаются без ошибок.
